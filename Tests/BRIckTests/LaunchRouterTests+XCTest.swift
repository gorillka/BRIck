import XCTest

extension LaunchRouterTests {
    static var allTests: [(String, (LaunchRouterTests) -> () throws -> Void)] {
        [
            ("test_launchFromWindow", test_launchFromWindow)
        ]
    }
}
